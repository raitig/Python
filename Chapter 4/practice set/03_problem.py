a = (34,264,987,"Raitig")
a [2] = 45
print = (a) # Tuple cann't be change because tuple is immutable data type. If we want to change the value of tuple then we have to convert it into list and then we can change the value of list and then we can convert it into tuple.