a = {
    "Raitig" : 90, # Dictonary have key and values. In left side of dictonary we call keys and the right side of dictonary we call values. we can access the value by using the key. if we try to access the value by using the key which is not present in the dictonary then it will give us an error.
    "Rohit" : 80,
    "Virat" : 70,
    "Gourab" : 60,
    "Bijoy" : 50
}
# print(a.items())
# print(a.keys())
# print(a.values())
# a.update({"Virat" : 75}) # we can update the value of the key by using the update() method. we can also add new key and value by using the update() method.
# print(a)
#print(a.get("Rohit2")) # we can also access the value by using the get() method. if we try to access the value by using the key which is not present in the dictonary then it will return None. we can also provide a default value to return if the key is not present in the dictonary.
# Print None if the key is not present in the dictonary
#print(a["Rohit2"]) # Print error if the key is not present in the dictonary. we can also provide a default value to return if the key is not present in the dictonary.
# a.pop("Gourab") # we can remove the key and value by using the pop() method. we can also remove the key and value by using the del keyword.
# print(a)
# a.popitem() # we can remove the last key and value by using the popitem() method. we can also remove the last key and value by using the del keyword.
# print(a)
len(a)
print(len(a)) # we can find the length of the dictonary by using the len() method. len() method will return the number of key and value pairs in the dictonary. len() method will return the number of items in the dictonary. len() method will return the number of key and value pairs in the dictonary. len() method will return the number of items in the dictonary. len() method will return the number of key and value pairs in the dictonary. len() method will return the number of items in the dictonary. len() method will return the number of key and value pairs in the dictonary. len() method will return the number of items in the dictonary. len() method will return the number of key and value pairs in the dictonary. len() method will return the number of items in the dictonary. len() method will return the number of key and value pairs in the dictonary. len() method will return the number of items in the dictonary.