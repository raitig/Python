s = {1,2,3,4,5,5,6,8,8,7,"Raitig"}
#print(s,type(s)) # Set is unordered and unindexed collection of data. Set is mutable but we can't change the value of set but we can add or remove the value of set. Set is also unindexed so we can't access the value of set by using index. Set is also unchangeable but we can add or remove the value of set. Set is also unhashable so we can't use set as a key in dictionary. Set is also iterable so we can use for loop to iterate through the set. Set is also a collection of unique elements so it will not allow duplicate values. Set is also a collection of unordered elements so it will not maintain the order of elements. Set is also a collection of mutable elements so we can add or remove the value of set. Set is also a collection of unindexed elements so we can't access the value of set by using index. Set is also a collection of unchangeable elements so we can't change the value of set but we can add or remove the value of set. Set is also a collection of unhashable elements so we can't use set as a key in dictionary. Set is also a collection of iterable elements so we can use for loop to iterate through the set.
# s.add(100) # we can add the value to the set by using the add() method. we can also add the value to the set by using the update() method. we can also add the value to the set by using the union() method.
# s.update({100,200})
s.remove(5) # we can remove the value from the set by using the remove() method. we can also remove the value from the set by using the discard() method. we can also remove the value from the set by using the pop() method.
print(s,type(s))