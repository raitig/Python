name = "Raitig Sarkar" #in double quotes this is an string.
print(name[0:3]) #this is called slicing. it will print the first three characters of the string.
print(name[-10: -1]) #this is called slicing. it will print the last three characters of the string. because the index starts from -1.
print(name[-10: -1]) #this is called slicing. it will printthe last three characters of the string. because the index starts from -1.
print(name[:3]) #this is called slicing. it will print the first three characters of the string.
print(name[1:])