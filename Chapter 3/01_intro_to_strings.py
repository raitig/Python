# name = "Raitig" #in double quotes this is an string.
# me = len(name)
# # print(me)

# name = 'Raitig' #in single quotes this is also an string.
# name1 = name[0:3] #this is called slicing. it will print the first three characters of the string.
# print(name1)
# charecter = name[1]
# print(charecter) #this will print the second character of the string. because the index starts from 0.  

hm = "Raitig Sarkar"
mh = hm[-4: -1] #this is called slicing. it will print the last three characters of the string.
print(mh) #this will print the last three characters of the string. because the index starts from -1.   