n : int = 5

name : str = "Raitig"


def sum(a : int, b : int) -> int: # In function definition, we can specify the type of the parameters and the return type using type hints. Here, 'a' and 'b' are both integers, and the function returns an integer as well.
    return a + b
